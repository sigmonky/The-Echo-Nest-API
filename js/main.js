//testing api calls with this for now as they are created as collections and models
require(['utils/zepto','models/artist.model','views/artist.view'],function( $, ArtistM, ArtistV ){
 		
	var bioView = new ArtistV.SingleBio({name:"radiohead"});
	
	  
});